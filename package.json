{
  "name": "platformer",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "format": "prettier -w .",
    "lint": "prettier -l . && eslint ."
  },
  "devDependencies": {
    "@web/test-runner": "^0.18.0",
    "eslint": "^8.56.0",
    "prettier": "^3.2.4"
  }
}
